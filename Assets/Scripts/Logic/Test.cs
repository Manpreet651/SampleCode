using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Server.Data;
using UnityLibrary.Additions;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

//		if (Input.GetKeyDown (KeyCode.L)) {
//			ServerPlatform.Instance.LoadUserData ();
//		}
//		if (Input.GetKeyDown (KeyCode.P)) {
//
//			DebugClass.LogRed (GameFlow.Instance.PlayerProfile.Id + " id " + GameFlow.Instance.PlayerProfile.Name + " name " + GameFlow.Instance.PlayerProfile.URL);
//			//GameFlow.Instance.PlayerProfile.
//
//
//		}if (Input.GetKeyDown (KeyCode.S)) {
//			
//			DebugClass.LogGreen (GameFlow.Instance.PlayerProfile.SavedData.PlayerStatisticSavedData.avarageSpeed + " == "+GameFlow.Instance.PlayerProfile.Statistic.AvarageSpeed);
//
//
//		}
	}
}
